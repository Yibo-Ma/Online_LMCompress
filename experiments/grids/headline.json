{
  "group_name": "text_headline",
  "note": "Stage-3 final numbers: frozen Stage-1 winner, headline (plateau) stream size, swept over ALL test datasets. Compress-only for the rate table; confirm one run losslessly with best_config.json (mode both, no_decompress false).",
  "modality": "text",
  "fixed": {
    "model": "checkpoints/Qwen2.5-0.5B",
    "max_bytes": 256000000,
    "chunk_size": 2048,
    "train_interval": 8,
    "epochs_per_train": 2,
    "lr": 1e-04,
    "lora_r": 16,
    "lora_alpha": 32,
    "target_modules": "q_proj,k_proj,v_proj,o_proj",
    "batch_chunks": 8,
    "base_seed": 42,
    "lora_dropout": 0.0,
    "grad_clip": 1.0,
    "weight_decay": 0.01,
    "train_on_all": false,
    "no_decompress": true
  },
  "grid": {
    "data": [
      "data/text/normalized/qwen2.5/pile_of_law_eurlex",
      "data/text/normalized/qwen2.5/medal",
      "data/text/normalized/qwen2.5/enwik8"
    ]
  }
}
